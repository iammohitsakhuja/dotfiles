-- This file allows lazy.nvim to auto-discover all plugin files in the plugins/ directory.
-- When using `{ import = "plugins" }` in lazy.nvim setup, it will automatically load
-- all .lua files in this directory (except init.lua itself).
--
-- Each plugin file should return a plugin spec (table) directly.

return {}
